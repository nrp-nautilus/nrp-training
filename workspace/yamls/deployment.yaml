# Simple replicated Deployment (no Service/Ingress yet — see ingress-demo.yaml).
# Replace <username> in name + label. Apply with: kubectl apply -f deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-deploy-<username>
  namespace: nrp-training-k8s
  labels:
    app: hello-deploy-<username>
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-deploy-<username>
  template:
    metadata:
      labels:
        app: hello-deploy-<username>
    spec:
      containers:
      - name: hello
        image: nginxdemos/hello:plain-text
        ports:
        - containerPort: 80
        resources:
          # nginx starts one worker per host CPU (it does not see the cpu limit),
          # and NRP has nodes with 256 cores — 128Mi OOMKills there.
          limits:  { memory: 512Mi, cpu: 200m }
          requests: { memory: 512Mi, cpu: 200m }
